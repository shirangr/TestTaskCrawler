@page "{id:int?}"
@using TestTaskCrawler.Models;
@model TestTaskCrawler.Models.Product


<div class="text-center">
    <h1 class="display-4">Search Product</h1>
    <p>please insert a url address and get product details</p>
</div>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">

        <form class="form-inline my-2 my-lg-0" asp-action="SearchProduct">
            @Html.TextBoxFor(model => model.ProductURL, new { @class = "form-control center", @placeholder = "Product URL" })
            @Html.ValidationMessageFor(model => model.ProductURL)
            <input class="form-control mr-sm-2" name="ProductURL" id="searchline" type="search" placeholder="Search" aria-label="Search">
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
            <br />
            <br />
            <table class="table">
                <thead class="thead-light">
                    <tr>
                        <th>Product URL</th>
                        <th>Name</th>
                        <th>Description</th>
                        <th>Condition</th>
                        <th>Price</th>
                        <th>ShippingPrice</th>
                        <th>ImagePath</th>
                        <th>LastViewed</th>
                        <th>BackgroundPageColor</th>
                    </tr>
                </thead>
                <tbody>
                    @*@if (Model == null)
                        {

                        }
                        else
                        {
                            if (Model == null)
                            {

                            }
                            else
                            {
                                foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.ProductURL</td>
                                        <td>@item.Name</td>
                                        <td>@item.Description</td>
                                        <td>@item.Condition</td>
                                        <td>@item.Price</td>
                                        <td>@item.ShippingPrice</td>
                                        <td>@item.ImagePath</td>
                                        <td>@item.BackgroundPageColor</td>
                                    </tr>
                                }
                            }

                        }*@
                </tbody>
            </table>
        </form>


    </div>
</nav>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}